# Schema Pointer

The canonical schema lives at [`../../../wiki/SCHEMA.md`](../../../wiki/SCHEMA.md).

Keep entity types, frontmatter rules, confidence values, source-type templates, and related-page labels there so every workflow uses the same contract.
