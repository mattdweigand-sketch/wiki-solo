Run the lint workflow. The lint workflow lives in [`workspaces/maintenance/CONTEXT.md`](../../workspaces/maintenance/CONTEXT.md). Read that and follow the "Lint the wiki" task. Criteria are in [`workspaces/maintenance/docs/lint-criteria.md`](../../workspaces/maintenance/docs/lint-criteria.md). This command is just a Claude Code shortcut; the workflow files are canonical and agent-neutral.
