---
description: Ingest a new source from raw/ into the wiki
---

Run `wiki-ingest` through the canonical wiki workflow. Read `AGENTS.md`, then `CONTEXT.md`, then `workflows/ingest/CONTEXT.md`, and follow the routed Load / Skip list exactly.
This wrapper is generated from `scripts/wiki-wrapper-contract.json`; canonical behavior lives in `workflows/`.

$ARGUMENTS
