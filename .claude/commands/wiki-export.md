---
description: Export a zip backup of the wiki corpus, including raw sources
---

Run the export workflow. Read `workflows/maintenance/CONTEXT.md`, then `workflows/maintenance/export.md`, and follow the Load / Skip list exactly.

This command is just a Claude Code shortcut. The workflow file is canonical and model-agnostic.
