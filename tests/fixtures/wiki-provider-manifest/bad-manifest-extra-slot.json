{
  "id": "bad-manifest-extra-slot",
  "mutations": [
    {
      "set": [
        {
          "path": ["slots", "cloud_writer"],
          "value": {
            "supported_providers": ["stub"],
            "input_artifacts": ["packet.json"],
            "output_artifacts": ["writer.json"],
            "validator_command": "python3 scripts/wiki_validate_run.py <run-dir>",
            "writes_wiki": false,
            "human_review_required": true
          }
        }
      ]
    }
  ],
  "expected_errors": [
    "provider manifest has unsupported slots: cloud_writer"
  ]
}
